import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    ul: {
        justifyContent: 'space-around',
    },
    paginator: {
        borderRadius: 4,
        padding: '1em',
        margin: '1em 3em 0',
    },
}));