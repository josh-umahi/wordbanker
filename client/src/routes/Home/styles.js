import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    postsListing: {
        backgroundColor: "#f6f5f5",
        padding: "2em 0 1em",
    },
    outer: {
        marginBottom: "50px"
    }
}));