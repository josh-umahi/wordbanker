import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    postsListing: {
        backgroundColor: "#f6f5f5",
        padding: "2em 0 4em",
    },
    outer: {
        //TODO: Add this as something in @Home.tsx depends on it
    }
}));